const add = (num1, num2) => {
    return num1 + num2
}

const greeting = (name) => {
    return `Hello, ${name}.`
}

const returnTwo = () => {
    return 2
}

module.exports = {
    returnTwo,
    greeting,
    add
}